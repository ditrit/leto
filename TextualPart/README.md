#Prerequisite to use antlr parser
* Install antlr : `npm install antlr`
* Use compile to process the antlr4 grammar : `./compile leto.g4`
* Test the parser : `node index.mjs` 
* Debug with vscode : in menu "Run/Open Configurations", change from `index.js` to `index.mjs`



